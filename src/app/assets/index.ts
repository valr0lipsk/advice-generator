import { default as iconDice } from '!!raw-loader?!./icon-dice.svg';
import { default as dividerDesktop } from '!!raw-loader?!./pattern-divider-desktop.svg';
import { default as dividerMobile } from '!!raw-loader?!./pattern-divider-mobile.svg';

export const icons: Record<string, string> = {
  iconDice,
  dividerMobile,
  dividerDesktop,
};
